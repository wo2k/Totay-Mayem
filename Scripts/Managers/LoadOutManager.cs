using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LoadOutManager : SingletonTemplate<LoadOutManager>
{
    public List<GameObject> chatStickerInventory = new List<GameObject>();
    public List<GameObject> stickerSprite = new List<GameObject>();
  
    // Start is called before the first frame update
    void Start()
    {

  
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
